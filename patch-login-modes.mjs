import fs from 'node:fs';
const path='/home/ubuntu/pacca-vision/client/src/components/MockAuth.tsx';
let text=fs.readFileSync(path,'utf8');
text=text.replace('<div className="flex items-start justify-between"><div><div className="text-[10px] font-bold uppercase tracking-[.16em] text-blue-600">Welcome back</div><h2 className="mt-2 font-display text-2xl font-bold tracking-[-.05em]">Sign in to PACCA</h2><p className="mt-2 text-[11px] text-slate-500">Choose a workspace to continue.</p></div>', '<div className="mb-5 grid grid-cols-2 gap-2 rounded-xl bg-slate-100 p-1"><button onClick={() => setEntryMode("client")} className={cn("rounded-lg px-3 py-2 text-[10px] font-bold transition", entryMode === "client" ? "bg-white text-blue-700 shadow-sm" : "text-slate-500")}>Client Workspace</button><button onClick={() => setEntryMode("central")} className={cn("rounded-lg px-3 py-2 text-[10px] font-bold transition", entryMode === "central" ? "bg-[#142b4b] text-white shadow-sm" : "text-slate-500")}>Central Admin</button></div><div className="flex items-start justify-between"><div><div className="text-[10px] font-bold uppercase tracking-[.16em] text-blue-600">{entryMode === "central" ? "Platform administration" : "Client workspace access"}</div><h2 className="mt-2 font-display text-2xl font-bold tracking-[-.05em]">{entryMode === "central" ? "PACCA Admin Portal" : "Sign in to PACCA"}</h2><p className="mt-2 text-[11px] text-slate-500">{entryMode === "central" ? "Manage clients, deployments, and platform health." : "Choose a client workspace to continue."}</p></div>');
text=text.replace('<span className="mb-2 block text-[10px] font-bold text-slate-500">Workspace / tenant</span>', '<span className="mb-2 block text-[10px] font-bold text-slate-500">{entryMode === "central" ? "Platform scope" : "Workspace / tenant"}</span>');
text=text.replace('onClick={() => onLogin({ ...selected, tenant })}', 'onClick={() => onLogin({ ...selected, tenant: entryMode === "central" ? "PACCA Platform" : tenant })}');
text=text.replace('>Continue to workspace <ArrowRight size={15} />', '>{entryMode === "central" ? "Enter Admin Portal" : "Continue to workspace"} <ArrowRight size={15} />');
text=text.replace('<select value={tenant} onChange={(e) => setTenant(e.target.value)}', '<select disabled={entryMode === "central"} value={entryMode === "central" ? "PACCA Platform" : tenant} onChange={(e) => setTenant(e.target.value)}');
fs.writeFileSync(path,text);
console.log('login experience selector added');
